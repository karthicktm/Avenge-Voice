[project]
name = "agent-worker"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "livekit-agents[google]>=0.12.0",
    "livekit-plugins-silero>=0.7.0",
    "httpx>=0.27.0",
    "structlog>=25.0.0",
    "pydantic-settings>=2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
